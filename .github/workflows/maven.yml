name: Maven CI

on: [push, pull_request]

jobs:
  build:
    strategy:
      matrix:
        java: [8, 11]

    nae: Java ${{ matrix.java }} build
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up Java
      uses: actions/setup-java@v1
      with:
        java-version: ${{ matrix.java }}
    - name: Build with Maven
      run: mvn package --file pom.xml
